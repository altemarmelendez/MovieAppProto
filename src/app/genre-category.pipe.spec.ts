import { GenreCategoryPipe } from './genre-category.pipe';

describe('GenreCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
